# Wire vectors

Place profile-specific fixed byte vectors here. A vector should document its profile, logical input, expected wire bytes, and the specification section that defines the encoding.

The existing language tests are retained as compatibility coverage while vectors are extracted into this shared directory incrementally.
